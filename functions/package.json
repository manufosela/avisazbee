{
  "name": "avisazbee-functions",
  "version": "0.1.0",
  "description": "Cloud Functions de avisazbee: HTTPS desde Home Assistant + ciclo de vida de alertas.",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo 'lint pendiente'",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google-cloud/tasks": "^5.5.1",
    "bcrypt": "^5.1.1",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "google-auth-library": "^9.14.2"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
